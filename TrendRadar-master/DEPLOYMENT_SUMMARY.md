# TrendRadar + AI 智能搜索 部署总结

## 🎉 项目概述

TrendRadar 是一个智能热搜新闻聚合器，现已集成 AI 智能搜索功能，可以自动补充养老相关资讯。

### ✨ 核心特性

- ✅ **多平台热搜聚合**：支持 11 个主流平台（微博、知乎、抖音、B站等）
- ✅ **AI 智能搜索**：当热搜结果不足时，自动搜索养老相关新闻
- ✅ **智能筛选**：基于关键词和 AI 评分的双重过滤
- ✅ **多渠道推送**：支持飞书、钉钉、企业微信、Telegram、邮件
- ✅ **GitHub Actions 自动化**：无需服务器，自动定时运行

## 📊 功能架构

```
┌─────────────────────────────────────────────────┐
│            TrendRadar 主程序                     │
│                                                  │
│  ┌────────────────────────────────────────┐    │
│  │  1. 热搜数据抓取（11个平台）             │    │
│  │     - 微博、知乎、抖音、B站...           │    │
│  └────────────────────────────────────────┘    │
│                      ↓                          │
│  ┌────────────────────────────────────────┐    │
│  │  2. 关键词过滤                           │    │
│  │     - 必需词、频率词、过滤词             │    │
│  └────────────────────────────────────────┘    │
│                      ↓                          │
│  ┌────────────────────────────────────────┐    │
│  │  3. 结果数量检查                         │    │
│  │     < 3 条？ → 触发 AI 搜索             │    │
│  └────────────────────────────────────────┘    │
│                      ↓                          │
│  ┌────────────────────────────────────────┐    │
│  │  4. AI 智能搜索（可选）                  │    │
│  │     - Serper API（Google 搜索）         │    │
│  │     - Gemini AI 筛选                    │    │
│  │     - 自动补充养老资讯                   │    │
│  └────────────────────────────────────────┘    │
│                      ↓                          │
│  ┌────────────────────────────────────────┐    │
│  │  5. 结果合并与排序                       │    │
│  └────────────────────────────────────────┘    │
│                      ↓                          │
│  ┌────────────────────────────────────────┐    │
│  │  6. 多渠道推送                           │    │
│  │     - 飞书、钉钉、企业微信、Telegram...  │    │
│  └────────────────────────────────────────┘    │
└─────────────────────────────────────────────────┘
```

## 🔧 已完成的核心修改

### 1. AI 搜索模块 (`ai_search.py`)

**新增文件**，实现以下功能：

- ✅ Serper API 集成（Google 搜索）
- ✅ Gemini AI 智能筛选
- ✅ 搜索关键词优化（养老保险、政策、服务等）
- ✅ 自动重试和降级策略
- ✅ 代理问题修复
- ✅ Windows 编码问题修复

**关键代码**：
```python
class AISearchManager:
    def search_and_filter(self) -> List[Dict]:
        # 1. Serper API 搜索
        search_results = self._search_with_serper()
        
        # 2. Gemini AI 筛选（可选）
        filtered_results = self._filter_with_gemini(search_results)
        
        # 3. 格式化为统一格式
        return self._format_results(filtered_results)
```

### 2. 主程序集成 (`main.py`)

**修改内容**：

1. **代理问题修复**
```python
# 文件开头清除代理环境变量
os.environ.pop('HTTP_PROXY', None)
os.environ.pop('HTTPS_PROXY', None)
os.environ.pop('http_proxy', None)
os.environ.pop('https_proxy', None)

# DataFetcher 类中禁用代理
class DataFetcher:
    def __init__(self, proxy_url=None):
        self.session = requests.Session()
        self.session.trust_env = False  # 关键：禁用环境变量代理
```

2. **AI 搜索集成**
```python
def _supplement_with_ai_search(self, stats, ...):
    total_matched = sum(stat["count"] for stat in stats)
    threshold = CONFIG["AI_SEARCH"]["TRIGGER_THRESHOLD"]
    
    if total_matched < threshold:
        print(f"[警告] 热搜筛选结果仅 {total_matched} 条")
        print("[AI] 触发 AI 智能搜索...")
        
        ai_results = search_pension_news_with_ai(CONFIG)
        # 合并结果...
```

3. **编码问题修复**
- 替换所有 emoji 字符为文本标签（共 17 处）
- `⚠️` → `[警告]`
- `❌` → `[错误]`
- `✅` → `[成功]`
- `🤖` → `[AI]`

### 3. 配置文件 (`config/config.yaml`)

**新增配置节**：

```yaml
# AI 智能搜索配置
ai_search:
  enabled: true
  trigger_threshold: 3
  serper_api_key: ""  # 从环境变量读取
  gemini_api_key: ""  # 从环境变量读取
  search_keywords:
    - "养老保险政策"
    - "养老金调整"
    - "商业养老保险"
    - "个人养老金"
    - "养老服务"
    - "养老改革"
    - "退休金上调"
    - "养老体系"
  time_range_hours: 24
  max_results: 15
  gemini_model: "gemini-1.5-flash"
  relevance_threshold: 5
```

### 4. GitHub Actions 配置 (`.github/workflows/crawler.yml`)

**新增环境变量**：

```yaml
- name: Run crawler
  env:
    # AI 智能搜索 API 密钥
    SERPER_API_KEY: ${{ secrets.SERPER_API_KEY }}
    GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
    # ... 其他配置
```

### 5. 依赖管理 (`requirements.txt`)

**新增依赖**：

```txt
# AI 搜索依赖
google-generativeai>=0.8.0,<1.0.0
```

## 📁 新增文档

| 文档 | 说明 |
|------|------|
| `AI_SEARCH_README.md` | AI 搜索功能完整说明 |
| `AI_SEARCH_QUICKSTART.md` | 快速开始指南 |
| `GITHUB_ACTIONS_GUIDE.md` | GitHub Actions 详细配置指南 |
| `GITHUB_ACTIONS_CHECKLIST.md` | 配置检查清单 |
| `DEPLOYMENT_SUMMARY.md` | 本文档 |
| `IMPLEMENTATION_SUMMARY.md` | 实施总结 |
| `test_ai_search.py` | AI 搜索测试脚本 |

## 🔑 API 密钥

### Serper API

- **用途**：Google 搜索 API
- **申请**：https://serper.dev/
- **免费额度**：2500 次/月
- **你的密钥**：`3cab25879262b63b47afd75c9981d7286b453f97`

### Gemini API

- **用途**：AI 智能筛选
- **申请**：https://aistudio.google.com/app/apikey
- **免费额度**：每分钟 15 次请求
- **建议**：重新生成密钥（之前的已在对话中暴露）

## 🚀 部署方式

### 方式 1：GitHub Actions（推荐）

**优点**：
- ✅ 无需服务器
- ✅ 完全免费
- ✅ 自动定时运行
- ✅ 结果自动保存

**步骤**：

1. **Fork 项目到你的 GitHub**

2. **添加 Secrets**（Settings → Secrets and variables → Actions）
   ```
   SERPER_API_KEY = 你的密钥
   GEMINI_API_KEY = 你的密钥
   （可选）飞书/钉钉/企业微信等通知渠道
   ```

3. **启用 Actions**
   - 进入 Actions 标签页
   - 启用 "Hot News Crawler" workflow

4. **等待自动运行或手动触发**
   - 自动：每天北京时间 16:00
   - 手动：Actions → Run workflow

**详细指南**：[GITHUB_ACTIONS_GUIDE.md](GITHUB_ACTIONS_GUIDE.md)

### 方式 2：本地运行

**步骤**：

1. **安装依赖**
   ```bash
   pip install -r requirements.txt
   ```

2. **配置 API 密钥**
   
   方式 A：环境变量（推荐）
   ```bash
   # Windows PowerShell
   $env:SERPER_API_KEY="你的密钥"
   $env:GEMINI_API_KEY="你的密钥"
   
   # Linux/Mac
   export SERPER_API_KEY="你的密钥"
   export GEMINI_API_KEY="你的密钥"
   ```
   
   方式 B：直接在 `config.yaml` 中配置
   ```yaml
   ai_search:
     serper_api_key: "你的密钥"
     gemini_api_key: "你的密钥"
   ```

3. **运行程序**
   ```bash
   python main.py
   ```

### 方式 3：服务器部署

使用 cron 定时任务：

```bash
# 编辑 crontab
crontab -e

# 添加定时任务（每天 16:00）
0 16 * * * cd /path/to/TrendRadar && /usr/bin/python3 main.py
```

## 🧪 测试验证

### 本地测试

```bash
# 测试 AI 搜索功能
python test_ai_search.py

# 运行主程序
python main.py
```

### GitHub Actions 测试

1. 手动触发一次运行
2. 查看日志确认无错误
3. 检查 `output/` 目录是否生成结果

### 验证 AI 搜索

在日志中查找：

```
[警告] 热搜筛选结果仅 X 条，少于阈值 Y 条
[AI] 触发 AI 智能搜索补充养老资讯...
[AI] 智能搜索启动
[成功] Serper 搜索到 N 条结果
[完成] AI 搜索完成，共获取 M 条养老资讯
```

## ✅ 成功标志

部署成功后，你将看到：

1. **热搜抓取成功**
   ```
   获取 toutiao 成功（最新数据）
   获取 baidu 成功（最新数据）
   ...
   成功: [11个平台], 失败: []
   ```

2. **AI 搜索触发**（当热搜结果不足时）
   ```
   [警告] 热搜筛选结果仅 1 条
   [AI] 触发 AI 智能搜索...
   [完成] AI 搜索完成，共获取 8 条养老资讯
   ```

3. **结果生成**
   ```
   HTML报告已生成: output/2025年11月17日/html/当日汇总.html
   ```

4. **推送成功**（如果配置了通知）
   ```
   飞书消息发送成功
   ```

## ⚠️ 已解决的问题

### 1. 代理问题 ✅

**现象**：所有请求失败，提示 `check_hostname requires server_hostname`

**原因**：系统配置了代理但代理软件未运行

**解决**：
- 在代码开头清除代理环境变量
- 使用 `requests.Session()` 并设置 `trust_env = False`

### 2. 编码问题 ✅

**现象**：`UnicodeEncodeError: 'gbk' codec can't encode character`

**原因**：Windows 终端使用 GBK 编码，无法显示 emoji

**解决**：
- 将所有 emoji 替换为文本标签

### 3. 依赖安装问题 ✅

**现象**：`fastmcp` 安装失败（Python 3.9）

**原因**：`fastmcp` 需要 Python 3.10+

**解决**：
- GitHub Actions 使用 Python 3.10
- 本地可选择性安装（MCP 功能可选）

## 📊 资源消耗

### API 调用额度

| API | 免费额度 | 每次调用 | 估算使用 |
|-----|----------|----------|----------|
| Serper | 2500次/月 | 1次 | 每天1次 = 30次/月 |
| Gemini | 15次/分钟 | 1次 | 每天1次 = 30次/月 |

**结论**：完全在免费额度内

### GitHub Actions

- **公开仓库**：无限制 ✅
- **私有仓库**：2000 分钟/月（每次运行 1-3 分钟）

## 🎯 后续优化建议

### 1. 关键词优化

根据实际效果调整 `search_keywords`：

```yaml
search_keywords:
  - "养老保险政策"
  - "养老金调整"
  - "个人养老金账户"
  # 添加更多精准关键词
```

### 2. 触发阈值调整

```yaml
trigger_threshold: 3  # 根据需要调整（1-10）
```

- 设置为 1：只要有 1 条就不触发 AI 搜索
- 设置为 5：需要 5 条以上才不触发

### 3. 运行频率调整

编辑 `.github/workflows/crawler.yml`：

```yaml
schedule:
  - cron: "0 0,8 * * *"  # 每天2次：8:00, 16:00
```

### 4. 通知渠道配置

根据需要添加飞书、钉钉、企业微信等推送。

### 5. Gemini 替代方案

如果 Gemini 访问受限，可以考虑：
- 使用国内 AI（通义千问、DeepSeek）
- 完全依赖 Serper 搜索结果（已经很准确）

## 📚 相关文档

| 文档 | 内容 |
|------|------|
| [AI_SEARCH_README.md](AI_SEARCH_README.md) | AI 搜索详细说明 |
| [GITHUB_ACTIONS_GUIDE.md](GITHUB_ACTIONS_GUIDE.md) | GitHub Actions 配置指南 |
| [GITHUB_ACTIONS_CHECKLIST.md](GITHUB_ACTIONS_CHECKLIST.md) | 配置检查清单 |
| [readme.md](readme.md) | 项目主文档 |

## 🎊 总结

### ✅ 已完成

- [x] AI 智能搜索模块开发
- [x] 主程序集成
- [x] 代理问题修复
- [x] 编码问题修复
- [x] GitHub Actions 配置
- [x] 完整文档编写
- [x] 功能测试验证

### 🚀 可以投入使用

系统已经完全就绪，可以立即部署使用！

**快速开始**：
1. Fork 项目
2. 添加 API 密钥到 Secrets
3. 启用 Actions
4. 等待自动运行或手动触发

---

**需要帮助？**
- 查看详细文档
- 提交 [Issue](../../issues)
- 参考测试脚本

祝使用愉快！🎉



