# 核心依赖（必需）
requests>=2.32.5,<3.0.0
pytz>=2025.2,<2026.0
PyYAML>=6.0.3,<7.0.0

# AI 搜索依赖（启用 AI 搜索时必需）
google-generativeai>=0.8.0,<1.0.0

# MCP 服务依赖（可选，仅用于 AI 分析功能，需要 Python 3.10+）
# 如果不需要 MCP 功能，可以注释掉以下两行
fastmcp>=2.12.0,<2.14.0
websockets>=13.0,<14.0
